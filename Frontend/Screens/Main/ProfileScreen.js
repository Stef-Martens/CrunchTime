import * as React from 'react';
import { View,Text } from 'react-native';

export default function ProfileScreen({navigaition}){
    return (
        <View>
            <Text>ProfileScreen</Text>
        </View>
    );
}